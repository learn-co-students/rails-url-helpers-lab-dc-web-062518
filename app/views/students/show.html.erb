<h1><%= @student.to_s %></h1>
<h2><%= @student.active %></h1>
  <% if @student.active == true %>
  <label> "This student is currently active." </label>
  <% else @student.active == false %>
  <label> "This student is currently inactive." </label>

  <%end%>
